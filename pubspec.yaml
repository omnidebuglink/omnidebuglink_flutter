name: omnidebuglink
description: OmniDebugLink remote-debugging client SDK for Flutter apps.
version: 0.2.0
publish_to: none
environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.0
  shared_preferences: ^2.2.0
  vm_service: ^14.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
